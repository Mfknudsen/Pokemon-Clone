using UnityEngine;

namespace Mfknudsen.Files.Save_Types
{
    public class NPCSave : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
