using UnityEngine;

namespace Runtime.Weathers.Energy
{
    public class MagnetosphereWeather : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
